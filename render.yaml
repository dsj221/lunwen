services:
  - type: web
    name: lunwen
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    autoDeploy: true
    healthCheckPath: /api/health
